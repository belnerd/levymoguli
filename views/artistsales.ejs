<!-- View to show all artists sales -->
<%- include('partials/header'); %>
    </head>
    <body>
        <%- include('partials/navbar'); %>
        <div class="container">
            <% if (type === 'USER' ) { %>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Artisti</th>
                                <th>Levy</th>
                                <th>Myynti</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% for (var a of data) { %>
                                <tr>
                                    <td>
                                        <%= a.Artisti%>
                                    </td>
                                    <td>
                                        <%= a.Levy %>
                                    </td>
                                    <td>
                                        <%= a.Myynti %>
                                    </td>
                                </tr>
                                <%} %>
                        </tbody>
                    </table>
                </div>
                <% } %>
        </div>
    </body>
    </html>