<!-- View to show band sales by member -->
<%- include('partials/header'); %>
    </head>
    <body>
        <%- include('partials/navbar'); %>
        <div class="container">
            <% if (type === 'USER' ) { %>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Bändi</th>
                                <th>Levy</th>
                                <th>Myynti</th>
                                <th>Jäseniä</th>
                                <th>Myynti/jäsen</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% for (var a of data) { %>
                                <tr>
                                    <td>
                                        <%= a.Bandi%>
                                    </td>
                                    <td>
                                        <%= a.Levy %>
                                    </td>
                                    <td>
                                        <%= a.Myynti %>
                                    </td>
                                    <td>
                                        <%= a.Jasenia %>
                                    </td>
                                    <td>
                                        <%= a.osuus %>
                                    </td>
                                </tr>
                                <%} %>
                        </tbody>
                    </table>
                </div>
                <% } %>
        </div>
    </body>
    </html>